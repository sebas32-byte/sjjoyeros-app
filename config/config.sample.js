// Copia a `config/config.js` o a `js/config.js` según prefieras.
export const SUPABASE_URL = 'https://your-project.supabase.co';
export const SUPABASE_ANON_KEY = 'public-anon-key-replace-me';

// Para seguridad, añade este archivo a .gitignore y no lo subas a repositorios públicos.
